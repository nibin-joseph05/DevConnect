import { Slot } from 'expo-router';
import React from 'react';

/**
 * Leads folder layout - renders child page of the leads folder
 * Handles routing for lead management pages
 */
export default function LeadsLayout() {
  return <Slot />;
}
