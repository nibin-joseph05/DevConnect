import { Slot } from 'expo-router';
import React from 'react';

/**
 * Dashboard folder layout - renders child page of the dashboard folder
 * Handles routing for dashboard-related pages
 */
export default function DashboardLayout() {
  return <Slot />;
}
