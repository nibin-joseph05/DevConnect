import { Slot } from 'expo-router';
import React from 'react';

/**
 * Login folder layout - renders child page of the login folder
 * Handles routing for login-related pages
 */
export default function LoginLayout() {
  return <Slot />;
}
