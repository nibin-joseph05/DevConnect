import { Slot } from 'expo-router';
import React from 'react';

/**
 * Customers folder layout - renders child page of the customers folder
 * Handles routing for customer management pages
 */
export default function CustomersLayout() {
  return <Slot />;
}
