import { Slot } from 'expo-router';
import React from 'react';

/**
 * Register folder layout - renders child page of the register folder
 * Handles routing for registration-related pages
 */
export default function RegisterLayout() {
  return <Slot />;
}
